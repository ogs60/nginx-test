apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-test.fullname" . }}-version
data:
  version.json: |
    {
      "app": "nginx-test",
      "env": "uat",
      "version": "{{ .Chart.AppVersion }}"
    }